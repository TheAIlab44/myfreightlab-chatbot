// === history.js TEST VISIBILITE ===

// Création du bouton flottant
const historyButton = document.createElement('button');
historyButton.innerHTML = '🕑 Historique';
historyButton.style.position = 'fixed';
historyButton.style.top = '100px'; // Plus haut pour ne pas être masqué
historyButton.style.right = '30px';
historyButton.style.background = '#00478a';
historyButton.style.color = '#fff';
historyButton.style.border = 'none';
historyButton.style.padding = '12px 20px';
historyButton.style.borderRadius = '30px';
historyButton.style.boxShadow = '0 4px 8px rgba(0,0,0,0.3)';
historyButton.style.zIndex = '10000'; // Max pour passer devant tout
historyButton.style.cursor = 'pointer';
document.body.appendChild(historyButton);

// Création de la sidebar cachée au départ
const historySidebar = document.createElement('div');
historySidebar.style.position = 'fixed';
historySidebar.style.top = '0';
historySidebar.style.right = '-320px'; // caché
historySidebar.style.width = '300px';
historySidebar.style.height = '100%';
historySidebar.style.background = '#fff';
historySidebar.style.borderLeft = '2px solid #00478a';
historySidebar.style.boxShadow = '0 0 20px rgba(0,0,0,0.2)';
historySidebar.style.transition = 'right 0.3s ease';
historySidebar.style.zIndex = '9999';
historySidebar.innerHTML = `
  <div style="padding:20px;font-size:18px;font-weight:bold;">🗂️ Historique</div>
  <div id="history-content" style="padding:20px;">Aucun historique encore.</div>
`;
document.body.appendChild(historySidebar);

// Toggle ouverture/fermeture
let sidebarOpen = false;
historyButton.addEventListener('click', () => {
  historySidebar.style.right = sidebarOpen ? '-320px' : '0';
  sidebarOpen = !sidebarOpen;
});
