// === history.js : Sidebar Historique (visuel uniquement) ===

// IDs & classes
const historyPanelID = 'historyPanel';
const historyToggleID = 'toggleHistory';

// Création du bouton flottant à gauche
const historyButton = document.createElement('div');
historyButton.className = 'floating-toggle';
historyButton.id = historyToggleID;
historyButton.style.position = 'fixed';
historyButton.style.left = '0';
historyButton.style.top = '50%';
historyButton.style.transform = 'translateY(-50%)';
historyButton.style.background = '#00478a';
historyButton.style.color = 'white';
historyButton.style.padding = '12px';
historyButton.style.borderRadius = '0 8px 8px 0';
historyButton.style.cursor = 'pointer';
historyButton.style.zIndex = '1000';
historyButton.innerText = '📄'; // 📄 icône dossier/note

document.body.appendChild(historyButton);

// Création de la sidebar
const historySidebar = document.createElement('div');
historySidebar.id = historyPanelID;
historySidebar.style.position = 'fixed';
historySidebar.style.top = '0';
historySidebar.style.left = '-300px';
historySidebar.style.width = '300px';
historySidebar.style.height = '100%';
historySidebar.style.background = '#ffffff';
historySidebar.style.boxShadow = '2px 0 8px rgba(0, 0, 0, 0.1)';
historySidebar.style.transition = 'left 0.3s ease';
historySidebar.style.padding = '20px';
historySidebar.style.zIndex = '999';
historySidebar.innerHTML = `
  <h2 style="margin-top: 0;">Historique</h2>
  <p>Aucun historique pour le moment.</p>
`;

document.body.appendChild(historySidebar);

// Fonction d'ouverture/fermeture
let historyOpen = false;
historyButton.addEventListener('click', () => {
  historyOpen = !historyOpen;
  historySidebar.style.left = historyOpen ? '0' : '-300px';
});
